# 梁平鑫系統 — 開發維護規範 v6.1（2026/09/15）

> 本文件是系統的**唯一權威參考**。新增功能、修 bug、效能調校前先讀相關章節。
>
> **v6 變更：Email 提醒全面改為 PWA 推播**（新增第十五節）——
> `push.js` 前端註冊、`sw.js` push / notificationclick handler（現為 `lp-xin-v16`）、
> `push_notify.js` 推播 helper、`push_tokens` / `push_log` / `push_inbox` 三個集合、
> 各模組獨立推播時段、`alert_settings.html` 改版（時間設定＋綁定狀況）；
> `email_alert.js` 的 nodemailer 寄信段落已移除；`email_bind.html` 下線。
>
> **v6.1 修正（同日）**：點通知的導頁做法定案為「直接開功能頁」，
> 中途嘗試的 `Client.navigate()`、`postMessage`、`dashboard.html?go=` 三種
> 全部失敗，過程記錄於 15-9，**改 notificationclick 前務必先讀**。
>
> v5 變更：招募系統整合全部完成（第十三節）、Node.js 22 升級完成、
> 新增第十四節「金鑰與憑證管理」。

---

## 一、系統概觀

| 項目 | 說明 |
|---|---|
| 部署 | 靜態 HTML → GitHub Pages（repo: `eric0w0chn-hue/hr-system`） |
| 後端 | Firebase 專案 `liangpinghri`（Firestore + Auth + Storage + Cloud Functions） |
| 使用者 | 約 30 位管理職，**80%+ 手機存取** |
| 店面 | 約 14 間（含央廚與門市兩種 locationType） |
| CDN | GitHub Pages 上傳後 5–15 分鐘同步；`raw.githubusercontent.com` 即時 |
| Functions | **Node.js 22**，共 13 支，見 13-9 清單；每日備份 `dailyFirestoreBackup` UTC 17:00（台灣 01:00） |
| 通知 | **PWA Web Push（FCM）**，2026/09 取代 Email，見第十五節 |

### 檔案清單（2026/09 現況）

| 類型 | 檔案 |
|---|---|
| 主框架 | `dashboard.html`（SPA 主框架）、`index.html`（登入）、`welcome.html`（歡迎頁） |
| 核心 JS | `auth-guard.js`（權限守衛+Auth 借用）、`sw.js`（Service Worker，現為 `lp-xin-v16`）、**`push.js`（推播註冊）** |
| 人事 | `hr.html`、`batch_accounts.html` |
| 排班薪資獎金 | `schedule.html`、`salary.html`、`bonus.html` |
| 回報 | `price.html`、`cctv_report.html`、`delivery_report.html`、`meeting.html` |
| 帳號權限 | `account.html`、`store_accounts.html`、`permissions.html` |
| 系統 | `task.html`、`oplog.html`、`backup.html`、`restore.html`、`checkout.html`、**`alert_settings.html`（推播排程設定）** |

> 🗑 **2026/09/15 `email_bind.html` 下線**：Email 提醒已移除，聯絡信箱設定頁沒有用途。
> `dashboard.html` / `permissions.html` 的 `ALL_MODULES`、`DEFAULT_PERMISSIONS` 均已移除該筆。

> 🗑 **2026/07/16 打卡系統整組下線**：`clock / attendance / anomaly / anomaly_query /
> realtime / temp_clock / device_approval` 七頁已自 repo 刪除。
> Firestore 的 `attendance*` 集合資料仍在（無頁面引用，無害）。

---

## 二、SPA iframe 架構

```
dashboard.html（主框架，永遠顯示）
├── topbar（含 iframeActionBar + 使用者資訊 + 🔔推播鈕 + 登出）
├── sidebar（功能選單，分群組）
├── announce-bar（公告橫幅）
└── <iframe id="contentFrame">
      └── 功能頁（topbar 被隱藏）
```

- 側邊欄點擊 → `goTo(page)` → 設 iframe src（帶 `embed=1&v=N` 快取參數）
- `checkout.html` 例外：整頁跳轉，不進 iframe
- LOGO 點擊 → 回 welcome 頁，iframeActionBar 清空

### 2-1 Topbar 隱藏機制

iframe 載入後 dashboard 的 `frame.onload` 同源設定 `data-embed="1"`，
功能頁 CSS `[data-embed="1"] .topbar` 隱藏。

> ⚠️ **不能**用 URLSearchParams 判斷 embed——iframe 內會被瀏覽器安全政策 block。

功能頁**必加**的 CSS：
```css
[data-embed="1"] .topbar,[data-embed="1"] .topbar *{display:none!important;height:0!important;min-height:0!important;overflow:hidden!important}
```

> ⚠️ **salary.html 用的是另一種等效寫法**——頁面自己判斷 `window.self!==window.top`，
> 用 JS 內聯 style 隱藏 `#pageTopbar` 並調整 `.sheet-hd{top:0}`。
> 這是已知的合理特例，**不要用 `[data-embed]` CSS 覆蓋或「統一」它**。
> 新頁面預設仍用 `[data-embed]` CSS。

### 2-2 iframeActionBar（功能按鈕同步）

`_syncIframeActions(frame)` 掃描 iframe `.topbar-right` 的按鈕（排除 back-btn / role-chip），
在主框架建立代理按鈕，點擊轉發 `origBtn.click()`，MutationObserver 同步 disabled/文字。

目前同步的按鈕：oplog（匯出CSV/重新整理）、store_accounts（新增帳號）、
task（新增任務）、backup（立即備份）、permissions（儲存變更）、
alert_settings（儲存變更）。

### 2-3 返回按鈕（postMessage）

```javascript
onclick="if(window.self!==window.top){window.parent.postMessage({type:'navigate',page:'dashboard.html'},'*');}else{window.location.href='dashboard.html';}"
```

### 2-4 紅點提醒契約（dashboard 未完成事項）

dashboard 登入後背景執行 `checkUnfinishedAlerts()`，檢查：

| 檢查項 | 條件 | 依賴欄位 |
|---|---|---|
| task | store_tasks 有 pending/in_progress 且 dueDate < 今天 | `status`、`dueDate`、`store` |
| price | price_reports 今日轄下店面未填 | `date`（YYYY-MM-DD）、`store` |
| cctv | cctv_reports 今日轄下店面未填 | `date`、`store` |
| delivery | delivery_reports 明日央廚未填 | `deliveryDate`、店名欄位 |

> ⚠️ **新增回報類模組時**，若要納入紅點提醒，文件必須有 `date`（YYYY-MM-DD 字串）
> 與店名欄位，並在 `checkUnfinishedAlerts()` 加一段檢查。
>
> 紅點（頁面內）與推播（第十五節）是**兩套獨立機制**，判斷邏輯各自實作：
> 紅點在 `dashboard.html`、推播在 `functions/email_alert.js` 的 `CHECKERS`。
> 改判斷條件時**兩邊都要改**，否則會出現「有推播卻沒紅點」或反之。

---

## 三、Auth 與權限系統

### 3-1 auth-guard.js 三層放行策略（效能關鍵）

iframe 每次換頁是全新 context，Firebase Auth 重新初始化要等 1–2 秒。
解法：**借用主框架已就緒的登入狀態**。

```
① auth.currentUser 已有值（同頁重複呼叫）→ 直接放行
② window.parent.__lpAuth.currentUser 有值（iframe 借用主框架）→ 直接放行 ⭐ 換頁走這條
③ 都沒有（冷啟動）→ onAuthStateChanged 等待
```

配套：
- `dashboard.html` 初始化後執行 `window.__lpAuth = auth`（暴露給子頁）
- `auth-guard.js` 頂部也會 `window.__lpAuth = auth`（直接開功能頁時自保）
- 借用後仍掛一個 onAuthStateChanged 監聽登出事件
- token 若失效，Firestore 讀取會失敗 → catch → 導回登入頁（安全）

> ⚠️ 改 auth-guard.js 後**必須 bump sw.js 的 CACHE_NAME**（見第五節）。

**iframe 導向修正**：auth-guard 所有導回登入頁/無權限頁的跳轉，
在 iframe 內一律 `window.top.location.href`，避免畫中畫。

### 3-2 權限三層

| 層 | 負責 | 設定處 |
|---|---|---|
| role toggle | 哪些角色能進功能 | permissions.html |
| locationType | 央廚/門市/全部 | permissions.html |
| locations | 進去後看哪些店 | auth-guard 回傳，已過濾，直接用 |

- 快取：sessionStorage TTL 5 分鐘；permissions.html 儲存後清 `lp_perm_v1`
- admin/consultant 的 `userData.locations` 為空陣列 → 需全店清單讀 `hr/settings.workLocations`
- Firestore 無該 moduleKey 設定時，預設只有 admin 可進

> ⚠️ **`DEFAULT_PERMISSIONS` 只在 Firestore 完全沒有該 key 時生效。**
> `settings/permissions` 文件早就存在，新增模組後若沒進 permissions.html
> 儲存過一次，側邊欄不會出現該項目（2026/09 alert_settings 實際踩過）。
> **新增模組後務必進 permissions.html 打開開關並儲存。**

### 3-3 標準頁面入口

```javascript
import { authGuard, db, writeOpLog } from './auth-guard.js?v=2';
authGuard('moduleKey', ({ user, uid, role, locations, userData, db }) => {
  // locations 已過濾完畢；role: admin/consultant/manager/employee
});
```

### 3-4 模組 key 對照（現況）

hr、schedule（含 employee）、price、meeting、salary、annual_leave、bonus、account、
cctv、delivery_report（央廚限定）、oplog（admin）、permissions（admin）、backup（admin）、
checkout、store_accounts、task、cost、recruit104、**alert_settings**（admin）、email_bind、
store_manage、talent（外部招募系統，見第十三節）

---

## 四、Firestore 資料與查詢規範

### 4-1 主要路徑

| 模組 | 路徑 |
|---|---|
| hr | `hr/employees`（list 陣列，~136人 101KB）、`hr/settings`、`hr/interviews`、`hr/resigned` |
| salary | `salary_records/{empId}_{yyyy-mm}`、`salary_settings/config`、`salary_settings/store_ot` |
| schedule | `schedules/{yyyy-mm}_{location}`、`shift_types/{storeName}` |
| 回報 | `price_reports`、`cctv_reports`、`meeting_records`、`delivery_reports`、`delivery_personnel` |
| bonus | `hr/bonus_log` |
| 帳號 | `users/{uid}`、`store_accounts`、`settings/store_accounts_hidden` |
| 任務 | `store_tasks`（append-only updates 陣列） |
| 日誌 | `sys_oplogs`（主）、`hr_oplogs`（hr 雙寫，oplog 頁合併讀） |
| 設定 | `settings/permissions`、`settings/announcement`、`settings/delivery_vehicles`、`settings/stores`、`settings/alert_config` |
| **推播** | **`push_tokens/{uid}`、`push_log/{uid}_{date}_{module}`、`push_inbox/{uid}`**（見 15-3） |
| Storage | 任務照片 `task_photos/{taskId}/before\|after`；勿動既有 `storagePath` 欄位 |

外部整合：meeting 的 Google Sheets 同步走 Apps Script（URL 寫死在 meeting.html
`SHEETS_SCRIPT_URL`）。

### 4-2 查詢鐵律

**① 絕不全集合讀取後前端 filter**
```javascript
// ❌ getDocs(collection(db,'attendance')) 後 .filter(...)
// ✅ getDocs(query(collection(db,'attendance'), where('date','==',today)))
```

**② 絕不在迴圈裡逐一 await 讀取**（實測：13人序列 546ms → 並行 46ms）
```javascript
// ❌ for(const e of emps){ const rec = await loadRec(e.id); }
// ✅ const recs = await Promise.all(emps.map(e => loadRec(e.id)));
```

**③ 獨立讀取一律 Promise.all 並行**

**④ 店名不可用 where 比對**（舖/鋪 異體字會漏資料）
→ 用 `date` 在 Firestore 端壓縮資料量，店名留前端 `normLoc()` filter。

**⑤ 無條件查詢加 limit 上限保護**

### 4-3 寫入端契約

回報類（price/cctv/delivery）每筆需 `date`（YYYY-MM-DD 字串）＋店名欄位
（紅點提醒與推播都依賴，見 2-4 與 15-5）。新增任何「依日期查詢」的集合時一律比照：
`date` 字串欄位給查詢用、ISO 時間戳欄位給排序用。

### 4-4 複合索引

加 `where`+`orderBy` 首次查詢會報錯附建立連結，點擊等 1–2 分鐘。
在低流量時段做；索引建立期間該查詢會失敗。

---

## 五、Service Worker（sw.js）

| 資源 | 策略 |
|---|---|
| Firebase SDK（gstatic） | Cache First（版本固定，永久快取） |
| 本站 HTML/JS/CSS | Stale While Revalidate |
| Firestore/Auth API（googleapis） | 不介入，永遠走網路 |
| **Web Push** | **`push` / `notificationclick` handler，見 15-2、15-9** |

**維護規則：**
- 改 `auth-guard.js` 或想強制全站更新 → **bump `CACHE_NAME`**（現為 `lp-xin-v16`）
- **改 push handler 也必須 bump**，否則舊 SW 會繼續跑舊邏輯
- SWR 特性：使用者第一次載入可能仍是舊版，第二次才是新版
- 測試：`Ctrl+Shift+R` 強制重整；Console 檢查
  `caches.keys()` 應出現最新版本號
- **SW 換版卡住時**：DevTools → Application → Service Workers → **Unregister**
  再重整。反覆 bump 版本後舊 SW 可能卡在 waiting，`skipWaiting` 不一定救得回來
- **SW 的 `console.log` 不會出現在網頁 Console**。要看 SW 的 log：
  Application → Service Workers → 點 Source 那列的 `sw.js` 藍字 → 另開的視窗切 Console

---

## 六、Topbar 與 UI 規格

### 6-1 Topbar HTML（功能頁標配）

```html
<div class="topbar">
  <div class="topbar-left">
    <button class="back-btn" onclick="...postMessage 機制(見2-3)...">← 主選單</button>
    <div class="tb-dv"></div>
    <h1><img src="LOGO" style="width:28px;height:28px;border-radius:7px;">EMOJI 頁面名稱</h1>
  </div>
  <div class="topbar-right">
    <!-- 功能按鈕（選用） -->
    <span class="role-chip" id="roleChip">—</span>
  </div>
</div>
```

- 分隔線：新頁面**一律 `tb-dv`**。`.dv` 是舊頁遺留，hr.html 內容區用 `.dv` 會撞名
- role-chip 對照：admin=管理員、consultant=顧問、manager=店長/副店長、employee=同仁
- 功能按鈕放 role-chip 前 → 會被 iframeActionBar 自動同步到主框架

### 6-2 CSS 變數（全站統一）

```css
:root{--bg:#ECEEF2;--surface:#fff;--surface2:#F4F5F8;--border:#DDE1E9;--border2:#C8CDD8;
--text:#1A1D23;--text2:#5A6175;--text3:#9AA0B4;--accent:#2D3142;--accent-light:#F0F2F5}
```

### 6-3 RWD

- 單一斷點 600px；觸控目標 ≥44px
- 手機 table：thead 隱藏、tr 改 block、td 用 data-label
- Modal：手機 bottom-sheet（圓角上緣）、桌面置中
- iOS 陷阱：原生 select 不能多行 → 自訂下拉；onclick 內巢狀反引號會 crash iOS Safari；
  PWA safe-area 的 toast 用 opacity/visibility 不用 translateY
- `draggable="true"` 一律動態設定（mousedown 在把手才設），否則擋手機捲動

---

## 七、操作日誌（oplog）

```javascript
writeOpLog({ editor:currentUserName, role:currentRole, module:'moduleKey',
  action:'新增同仁' /*中文動詞+名詞*/, detail:'王小明' /*對象*/ });
```

- addDoc→新增、update/setDoc→編輯、deleteDoc→刪除、批次→記一次 detail 寫「N筆」
- 寫 `sys_oplogs`；hr.html 雙寫 `hr_oplogs`；oplog.html 合併讀＋去重

---

## 八、新增功能頁 SOP（七步）

1. **建功能頁 HTML**：用第六節 topbar + embed CSS + postMessage 返回 + authGuard 入口
2. **permissions.html** `ALL_MODULES` 加 `{key,icon,name,desc,page,active}`（先 grep 防重複）
3. **permissions.html** `DEFAULT_PERMISSIONS` 加 `新key:{roles:[...]}`
4. **dashboard.html** `ALL_MODULES` 加同一筆
5. **oplog.html** 補 dropdown `<option>` + `MODULE_MAP`（若會寫日誌）
6. **紅點提醒**（選用）：回報類模組要納入提醒 → dashboard `checkUnfinishedAlerts()` 加檢查；
   要納入**推播**再加 `email_alert.js` 的 `CHECKERS` + `alert_settings.html` 的項目列（見 15-5）
7. **permissions.html 介面設定** role toggle / 店面類型，**儲存**

> ⚠️ 第 7 步不能省。`DEFAULT_PERMISSIONS` 只在 Firestore 沒有該 key 時生效，
> 沒儲存過就不會出現在側邊欄（見 3-2 警告）。

---

## 九、AI 協作工作流程（給 Claude / 開發助手）

### 9-1 檔案版本鐵律
- **一律以使用者最新上傳的檔案或 repo raw 最新版為底**，絕不用 Claude 上次輸出的版本
- 抓檔帶 cache-bust：`curl "$RAW/檔案?_t=$(date +%s)"`
- 動手前先 diff / grep 確認基底版本含最新功能

### 9-2 修改與驗證
- 用 Python `assert old in content` 的字串替換，替換失敗立即發現
- 改完抽出 `<script>` 跑 `node --check`（module 區塊存成 `.mjs`）
- 加 ALL_MODULES / MODULE_MAP 前先 grep 防重複
- 薪資等數值邏輯改動需人工驗算公式再輸出

### 9-3 部署與快取
- 輸出到 `/mnt/user-data/outputs/`，使用者手動上傳 GitHub
- CDN 5–15 分鐘；驗證用 raw.githubusercontent.com
- 改 auth-guard.js 或 sw.js 的 push handler → bump sw.js CACHE_NAME
- 效能問題優先用 Claude in Chrome 實測，不要純看程式碼猜

### 9-4 已知設計（不是 bug，勿「修」）
- hr.html 保有第二個 Firebase 實例（建帳號用）
- hr.html 無 `#loading/#app` 切換結構（刻意）
- `dv` 分隔線在舊頁沿用（勿全站統一改）
- Storage `storagePath` 欄位勿動
- cctv 有 `EXCLUDE_CCTV_STORES` 排除清單；delivery 為央廚限定
- `email_alert.js` 的函式名稱是 `checkUnfinishedAndNotifyEmail`，
  **雖然已不寄 email 但不要改名**——改名等於刪舊建新，排程會斷

---

## 十、薪資與保險常數（2026）

- 正職公式：底薪 29,500 + 彈性補貼 + 固定加班費 = 目標薪資；不足月以 30 天為基準
- dailyBase 扣款用 BASE_FULL（全月基準），不用 prorated base
- 兼職：實領 = Σ(時數×(時薪+10全勤)) + 加班費(純時薪×倍率) + 加項 − 勞健保 − 扣點
- 雇主負擔：勞保=級距×8.94%、勞退=×6%、健保=級距×5.17%×60%×1.56
- 勞保投保天數：到職日～退保日**含頭尾**；退保日預設=離職當日（非+1）
- 級距表在 salary.html `LABOR_TABLE`，每年公告直接改表不動程式
- 投保級距模式：一般用 `getInsGrade`；**當月到職＋當月離職**自動切
  `same_month` 模式用 `getHireGrade`；`rec.gradeOverride` 手動覆蓋優先於一切
- 獎金補助（bonus.html）依到職日自動算年資，記錄寫 `hr/bonus_log`

---

## 十一、疑難排解速查

| 症狀 | 先查 |
|---|---|
| 換頁後 topbar 沒隱藏 | 該頁有無 `[data-embed]` CSS；dashboard `frame.onload` 是否最新 |
| 改了檔案但線上沒變 | CDN 未同步（等15分）；SW 快取（bump CACHE_NAME 或 Ctrl+Shift+R） |
| 換頁等 2 秒才有資料 | auth-guard 三層放行是否生效；`window.__lpAuth` 是否存在於主框架 |
| 查詢抓不到資料 | 寫入端欄位是否符合契約（date 格式）；店名異體字（normLoc） |
| **新模組側邊欄沒出現** | **permissions.html 有沒有儲存過（3-2）；sessionStorage 快取開新分頁** |
| Sheets 同步失敗 | meeting.html 的 `SHEETS_SCRIPT_URL` 是否為最新部署 |
| 權限改了沒生效 | sessionStorage 快取 TTL 5 分鐘 → 開新分頁 |
| 資料越來越慢 | 全集合讀取（4-2①）或迴圈 await（4-2②） |
| iOS 點擊爆版/無反應 | onclick 巢狀反引號；原生 select 多行；draggable 擋 touch |
| 首頁紅點不亮/亂亮 | 該模組文件的 date/store 欄位；normLoc 比對 |
| **推播收不到** | **見 15-7 專用排查表** |

---

## 十二、待辦

> ✅ **已結案**：Node.js 20→22 升級、招募系統管理員同步／單一登入／店家同步、
> **Email 提醒改 PWA 推播（2026/09/15）**。

| 項目 | 說明 |
|---|---|
| 推播綁定推廣 | 目前僅少數人綁定。`alert_settings.html` 下方可看「已綁定 N / M 人」，未綁定者排最前面。**未綁定的人現在完全收不到通知**（email 已移除）|
| firebase-functions 套件升級 | 部署時仍有 outdated 警告，無 deadline，低優先 |
| GMAIL secrets 清理 | `GMAIL_USER` / `GMAIL_APP_PASS` 已無函式使用，確定不回頭做 email 後可刪 |
| 桌面 Chrome 通知不顯示 | 開發機個案：`showNotification` 成功但 `getNotifications` 回 0，作業系統層攔截。手機正常，不影響上線 |
| 組織所有權遷移 | Google Workspace Super Admin 取得後：Firebase 專案移入 GCP Organization、帳單改公司、GitHub repo 移入 Org |
| Repo 遷移 | 移至 GitHub Org `Liangping-system`（低流量時段） |
| users 共用快取（選作） | account/store_accounts/cctv/schedule 各自全讀 users（~30筆），可在 auth-guard 加快取共用 |
| Firestore 打卡集合清理（選作） | `attendance*` 集合已無頁面使用 |
| `apple-mobile-web-app-capable` 警告 | dashboard.html 該 meta 已 deprecated，可加一行 `<meta name="mobile-web-app-capable" content="yes">`（不要刪舊的，舊版 iOS 還要） |

---

## 十三、招募系統整合

（內容同 v5，未變動。要點速記：）

- 兩個 Firebase 專案獨立不合併；資料流**單向**（招募 → 人資）
- `TALENT_STORE_MAP`（hr.html，storeId→店名）與 `TALENT_STORE_KEYWORDS`
  （functions/index.js，店名→storeId）**兩份要一起維護**
- 反向店名對照用 **startsWith 不用 includes**（「梁鑫雞肉飯」會同時對上
  `liangxin` 與 `xinjirou`）
- email 必須從 `hr/employees` 取，不能用 `users/{uid}.email`（後者是假信箱）
- 職稱為空時**完全省略 `title` key**，不要送空字串
- 跨網域 iframe 限制：`data-embed`、`__lpAuth` 借用、`_syncIframeActions` 全部無效
- SSO 走 `mintTalentToken` + postMessage，targetOrigin 不用 `'*'`
- 部署一律帶函式名稱 `--only functions:xxx`

### 13-9 Functions 清單（13 支，Node.js 22）

| 函式 | 區域 | 類型 | 用途 |
|---|---|---|---|
| `verifyMac` | us-central1 | onRequest | Omada MAC 驗證（打卡殘留）|
| `getSites` | us-central1 | onCall | Omada 站點清單 |
| `addToOmadaWhitelist` | us-central1 | onCall | Omada 白名單 |
| `checkDepartures` | us-central1 | onRequest | 未打卡下班檢查 |
| `onDeviceApproved` | asia-east1 | onDocumentWritten | 裝置核准後加白名單 |
| `dailyHrBackup` | asia-east1 | onSchedule | 人事資料每日備份（保留 30 天）|
| `cleanupOldCctvVideos` | asia-east1 | onSchedule | CCTV 影片 3 天清理 |
| `deleteAuthUser` | us-central1 | onCall | 刪除 Auth 帳號（admin）|
| `dailyFirestoreBackup` | asia-east1 | onSchedule | 全庫備份至 GCS |
| **`checkUnfinishedAndNotifyEmail`** | us-central1 | onSchedule | **未完成事項推播（每 5 分鐘，見第十五節）** |
| `importTalentApplications` | us-central1 | onCall | 招募系統求職者匯入代理 |
| `syncTalentAdmin` | asia-east1 | onDocumentWritten | 管理員同步 |
| `resyncAllTalentAdmins` | us-central1 | onCall | 管理員全量重推 |
| `mintTalentToken` | us-central1 | onCall | SSO Custom Token 簽發 |
| `syncTalentStores` | us-central1 | onCall | 店家名冊同步 |

> ⚠️ **部署一律帶函式名稱**。只有換 runtime 才整組部署，且做完必須確認三支排程還在。
>
> ⚠️ Firestore 觸發器區域跟著資料庫（asia-east1），callable 預設 us-central1。
> **看 log 要選對區域**。

---

## 十四、金鑰與憑證管理

### 14-1 現有 Secret 清單

| Secret 名稱 | 用途 | 使用的函式 |
|---|---|---|
| `TALENT_API_KEY` | 招募系統 API 共享密鑰 | `importTalentApplications`、`syncTalentAdmin`、`resyncAllTalentAdmins`、`syncTalentStores` |
| `TALENT_SA_KEY` | 招募系統 Service Account 金鑰（完整 JSON） | `mintTalentToken` |
| `OMADA_CLIENT_ID` / `OMADA_CLIENT_SECRET` | TP-Link Omada（打卡殘留） | Omada 系列函式 |
| `GMAIL_USER` / `GMAIL_APP_PASS` | ~~Email 提醒~~ | **已無函式使用**（2026/09 改推播），暫留 |

設定：`firebase functions:secrets:set 名稱`，之後重新部署使用該 secret 的函式。
Function 內用 `process.env.名稱` 讀取，並在 `onCall({ secrets:["名稱"] })` 宣告。

### 14-2 交付方式（重要）

**Service Account 金鑰等同該專案的完全管理權，且沒有到期日。**

> ⚠️ **2026/09/08 實際發生過**：對方透過通訊軟體以純文字傳送金鑰 JSON，
> 該金鑰已作廢並重新產生。

**允許**：密碼管理工具的安全分享（Bitwarden Send、1Password）、
對方遠端自行執行 `firebase functions:secrets:set`、當面 USB。

**禁止**：通訊軟體、email、雲端硬碟連結、貼進任何對話（**包含 AI 助手**）。

### 14-3 本機不留金鑰檔

設定進 Secret Manager 後**立即刪除本機 JSON**（含資源回收筒）。
特別注意**不要放在 `functions/` 資料夾**——會被 `firebase deploy` 整包上傳。

### 14-4 外洩時的處置順序

1. **先撤銷**：GCP Console → IAM → 服務帳戶 → 金鑰頁籤 → 刪除該 key ID
2. 產生新金鑰，用 14-2 方式交付
3. 重設 secret 並重新部署
4. 檢查該期間的異常存取紀錄

### 14-5 與外部系統的密鑰原則

- **repo 是 Public**，密鑰放前端等於公開 → 一律由 Cloud Function 代理
- Function 內另做角色檢查（讀 `users/{uid}.role`），光有登入不夠
- 我方**不提供** `liangpinghri` 的 SA 金鑰給對方
- **VAPID public key 例外**：`push.js` 裡的那串是公開金鑰，設計上就給瀏覽器看，
  放 public repo 沒有風險（私鑰在 Firebase 端）

---

## 十五、PWA 推播（2026/09 新增，取代 Email）

### 15-1 整體流程

```
使用者在 dashboard 按 🔔
  → Notification.requestPermission()
  → getToken(messaging, { vapidKey, serviceWorkerRegistration: 現有 SW })
  → token 寫入 push_tokens/{uid}.tokens[]

排程 checkUnfinishedAndNotifyEmail（每 5 分鐘）
  → 逐模組判斷「現在是否已過該模組的推播時間」
  → 到點的模組解析收件人 → notifyUser()
  → admin.messaging().sendEachForMulticast()
  → sw.js 的 push handler 呼叫 showNotification()
  → 使用者點通知 → sw.js 的 notificationclick → openWindow 直接開該功能頁
```

### 15-2 sw.js 的 push handler（關鍵坑）

> ⚠️ **標準 Web Push 中，瀏覽器不會自動顯示任何通知。**
> FCM 的 `notification` payload 之所以「自動顯示」，是靠 `firebase-messaging`
> 的 **SW SDK** 代為呼叫 `showNotification()`。
>
> 本專案**沒有** `importScripts` 那套 SDK（見 15-4 的路徑問題），
> 所以 `sw.js` **必須自己呼叫 `showNotification()`**，
> 不能寫成「偵測到 notification 欄位就 return 交給瀏覽器」——那樣什麼都不會顯示。
> （2026/09/15 實際踩過，浪費了數小時）

handler 同時支援兩種 payload：`d.notification`（FCM 標準）與 `d.data`（自訂），
取值順序 `n.title || p.title`。點擊後導向
`dashboard.html?go={page}`，由 dashboard 的 `?go=` 處理器開對應功能頁。

### 15-3 三個 Firestore 集合

| 集合 | 內容 | 寫入者 | 規則 |
|---|---|---|---|
| `push_tokens/{uid}` | `tokens[]`（一人多裝置）、`ua`、`updatedAt` | 前端 `push.js` | 本人可讀寫 |
| `push_log/{uid}_{date}_{module}` | 當日去重記錄 | Function | 前端不可寫，admin 可讀 |
| `push_inbox/{uid}` | 完整清單 `lines[]`、`title`、`date`、`readAt` | Function | 本人可讀、只能改 `readAt` |

> ℹ️ `push_inbox` 目前**沒有在前端使用**。原本設計是點通知回 dashboard 跳彈窗顯示
> 完整清單，後來改為直接開功能頁（15-9），彈窗已停用。
> `dashboard.html` 的 `_showPushInbox()` 函式保留未刪，觸發那行被註解掉，
> 日後要恢復只要取消註解。Function 端仍持續寫入這個集合。

> ⚠️ **`push_tokens` 刻意不放進 `users/{uid}`。**
> `users` 的 write 規則要求 `isStaff()`，同仁自己按 🔔 會被擋。

> ⚠️ **`push_log` 是測試時最容易卡住的地方。**
> `notifyUser()` 讀到當日記錄就直接 `return true`，**連 FCM 都不呼叫**。
> 症狀是 `lastSentPush` 有數字、log 卻沒有 `[push]` 那行，看起來像「推了但沒收到」。
> 重測同一模組前**必須刪掉** `push_log/{uid}_{date}_{module}`。
> （測不同模組就沒這問題，去重鍵含模組名）

### 15-4 前端註冊（push.js）

> ⚠️ **不要另開 `firebase-messaging-sw.js`。**
> FCM 預設去抓**根目錄** `/firebase-messaging-sw.js`，但 GitHub Pages 是子路徑
> `/hr-system/`，必定 404。
> 解法：`getToken()` 時把現有的 SW registration 傳進 `serviceWorkerRegistration`。

其他要點：
- messaging SDK 採**按需動態 import**，不支援的裝置完全不下載
- 已授權時自動靜默刷新 token（換手機、token 過期會自動補回）
- **iOS 必須先「加入主畫面」**，Safari 分頁內按不出通知權限（Apple 限制），
  `push.js` 偵測到 iOS 且非 standalone 會跳教學提示

### 15-5 各模組獨立推播時段

設定在 `settings/alert_config`：

```javascript
{
  enabled: true,
  items:      { task:true, price:true, cctv:true, delivery:true, bonus:false },
  checkTimes: { task:'10:00', price:'14:00', cctv:'21:00', delivery:'18:00', bonus:'09:00' },
  // 以下由 Function 回寫
  lastSentAt, lastSentPush, lastSentMissed, lastSentModules, lastSentDate
}
```

- 由 `alert_settings.html` 維護（admin 限定）
- **每模組獨立推播、不彙整**——一人一天可能收到多則
- `checkTimes` 缺漏時回退到舊的單一 `checkTime`，再回退到程式內 `DEFAULT_TIMES`
- 判斷式：`nowHM < hm` 就跳過；當日去重交給 `push_log`

**新增推播項目的步驟**：
1. `email_alert.js` 的 `CHECKERS` 加一筆 `{ label, page, run }`
   —— `page` 是點通知後要開的功能頁檔名，漏填會退回 `dashboard.html`
2. `alert_settings.html` 的 `KEYS` 與 `DEFAULT_TIMES` 各加一項
3. `alert_settings.html` HTML 加一列（`item_{key}` + `time_{key}`）

### 15-6 收件人解析

`resolveRecipients(moduleKey, storeName, globalOnly)`：
- 讀 `settings/permissions` 的 `modules[key].roles` 決定誰有資格
- admin 走全域清單（看到所有店），其餘依 `locations` 過濾
- **依 uid 去重**（不是 email）

> ⚠️ **`alertEmail` 的前置條件已於 2026/09/15 移除。**
> 原本要求使用者有填 `alertEmail` 才納入收件人，那是 email 時代的條件。
> `email_bind.html` 下線後新進管理職沒有地方設定它，再擋就會變成
> 「開了推播也永遠收不到」。現在改由 `notifyUser` 判斷——沒有 push token
> 自然推不到，不需要前置條件。`users/{uid}.alertEmail` 欄位保留未刪。

> ⚠️ 某模組的 `modules[key].roles` 若不含 `admin`，該模組的全域收件人會是空的。
> 2026/09 測試 `task` 模組時 `lastSentCount: 0` 就是這個原因（不是推播壞了）。

### 15-7 推播排查表

| 症狀 | 先查 |
|---|---|
| 完全沒觸發，`lastSentAt` 沒更新 | `enabled` 是否 true；該模組 `items[key]` 是否 true；`nowHM` 是否已過 `checkTimes[key]` |
| `lastSentPush` 有數字但沒有 `[push]` log | **`push_log` 當日記錄還在**（15-3），刪掉再測 |
| `lastSentCount/Push` 都是 0 | 該模組沒有未完成事項，或收件人解析為空（15-6） |
| log `ok=N` 但裝置沒跳 | iOS 未加入主畫面；`sw.js` 版本是否為含 push handler 的版本；系統通知設定 |
| `showNotification` 成功但畫面沒有 | Console 跑 `r.getNotifications()` 看數量；回 0 = 作業系統層攔截（非程式問題）|
| **點通知沒跳到功能頁** | **多半是開在新分頁而你在看舊分頁（15-9）；`CHECKERS[key].page` 有沒有填** |
| SW 改了沒生效 | Unregister 再重整（第五節）；`caches.keys()` 確認版本號 |
| 某人收不到 | `alert_settings.html` 看綁定狀況；或該人沒有 `alertEmail`（15-6）|

### 15-8 Email 移除後的影響

`email_alert.js` 的 nodemailer 段落已移除，**未開啟推播的人完全收不到通知**。
Function 會在 log 印出名單：

```
[alert] 以下 N 筆未推達（未開啟推播）：王小明(price)、李小華(cctv)
```

`mailQueue` 的資料結構**刻意保留未動**，日後要恢復 email fallback，
在 ② 區塊接回 `transporter.sendMail` 即可。

### 15-9 點通知的導頁（2026/09/15 定案，改之前先讀）

**最終做法**：`sw.js` 的 `notificationclick` 直接 `openWindow` 開該功能頁。

```javascript
const page = (event.notification.data && event.notification.data.page) || 'dashboard.html';
const target = new URL('./' + page + '?_n=' + Date.now(), self.location.href).href;
event.waitUntil(self.clients.openWindow(target));
```

功能頁本來就能獨立開啟（自帶 topbar + authGuard + 返回鈕），不需要繞 dashboard。
`?_n=` 時間戳確保每次都是不同網址，避免瀏覽器視為「網址沒變」而不重新載入。

> ⚠️ **點通知會開新分頁。** 桌面測試時如果 DevTools 開在原本的 dashboard 分頁上，
> 會看到「還停在主畫面」而誤判失敗——**功能頁在旁邊的新分頁裡**。
> 2026/09/15 因為這個誤判反覆改了六版 SW，浪費數小時。
> **測試時先確認 Chrome 有沒有多出分頁，再判斷成功與否。**

**以下三種做法都試過且失敗，不要再繞回去：**

| 做法 | 失敗原因 |
|---|---|
| `Client.navigate(target)` | 目標分頁網址若與當前相同（例如已經是 `?go=price.html`），瀏覽器視為無變化，不重新載入，`load` 不觸發 |
| `client.postMessage()` + dashboard 監聽 | 訊息送出了（SW log 看得到），dashboard 端收不到。加了 `startMessages()` 與 `onmessage` 屬性仍無效 |
| `dashboard.html?go=xxx` 由 dashboard 轉開 | 可行但繞遠路，且 `clients.matchAll()` 會把 iframe 也算成 client，導到 iframe 會變成 dashboard 裡再開 dashboard 無限套疊 |

**`goTo()` 的保護（一併保留）**：
`goTo('dashboard.html')` 會把主框架塞進 iframe 造成套疊。
現在 `goTo` 收到 `dashboard.html` / `index.html` 一律轉成 `welcome.html`，
行為與返回鈕的 postMessage 處理一致。**這段不要拿掉。**

---

---

## 附錄：本次異動檔案清單（2026/09/15）

| 檔案 | 位置 | 異動 |
|---|---|---|
| `push.js` | repo 根目錄 | **新增**——推播註冊；鈴鐺鈕只放圖示（文字會擠壞 topbar）|
| `sw.js` | repo 根目錄 | push / notificationclick handler；`lp-xin-v6` → **`v16`** |
| `dashboard.html` | repo 根目錄 | 🔔 鈕、`goTo` 防套疊、移除 `email_bind`、停用 `push_inbox` 彈窗 |
| `alert_settings.html` | repo 根目錄 | 各項目獨立時間；綁定狀況由 Email 改推播 |
| `permissions.html` | repo 根目錄 | 模組描述文字（LINE → 推播）；移除 `email_bind` |
| `email_bind.html` | repo 根目錄 | **刪除** |
| `push_notify.js` | `functions/` | **新增**——推播 helper（含當日去重、失效 token 清理）|
| `email_alert.js` | `functions/` | 移除 nodemailer；逐模組獨立時段；移除 `alertEmail` 前置條件 |
| Firestore 規則 | Console | 新增 `push_tokens` / `push_log` / `push_inbox` |
